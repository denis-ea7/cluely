# Деплой Cluely на сервер 109.61.108.37

## 📋 Что развернуто

1. **Postgres БД** (порт 5432) - база данных для пользователей
2. **Backend API** (порт 4000) - API для авторизации и выдачи ключей
3. **Key-Agent** (порт 8089) - сервис для получения ключей AI
4. **Cluely Site** (порт 3005) - сайт для регистрации/авторизации

## 🔑 Ключи встроены

Все ключи из `del2.js` и `.env` автоматически встроены в `deploy/.env`:

- ✅ PRIMARY_TOKEN, PRIMARY_WS_URL, PRIMARY_CHAT_URL, PRIMARY_MODEL (из del2.js)
- ✅ GEMINI_API_KEYS (из .env)
- ✅ OPENAI_API_KEYS (из .env)
- ✅ Proxy настройки (из .env)

## 🚀 Быстрый старт

### На сервере:

```bash
# 1. Подключиться
ssh denis@109.61.108.37
# Пароль: wss81lv9

# 2. Перейти в директорию
cd ~/cluely/free-cluely/deploy

# 3. Запустить
./deploy.sh
```

Или вручную:
```bash
docker compose up -d --build
```

## 📝 Настройка клиентского приложения

После деплоя, в Electron приложении создайте `.env`:

```bash
KEY_AGENT_URL=http://109.61.108.37:8089
SITE_URL=http://109.61.108.37:3005
DEEPLINK_SCHEME=cluely
```

## 🔍 Проверка работы

```bash
# API
curl http://109.61.108.37:4000/health

# Key-Agent
curl http://109.61.108.37:8089/health

# Site
curl http://109.61.108.37:3005
```

## 📚 Документация

- **QUICK_DEPLOY.md** - быстрая инструкция по деплою
- **DEPLOY.md** - подробная инструкция с troubleshooting

## ⚠️ Важно

1. Измените пароли БД в `.env` перед production
2. Измените `KEYS_SECRET` и `JWT_SECRET` в `.env`
3. Настройте firewall для портов 4000, 8089, 3005

## 🛠 Управление

```bash
# Логи
docker compose logs -f

# Остановка
docker compose down

# Перезапуск
docker compose restart

# Обновление
git pull  # или загрузить новую версию
docker compose up -d --build
```

