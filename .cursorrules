Не добавляй комментарии в код при генерации или редактировании. Код должен быть самодокументируемым через понятные имена переменных и функций.

Дополнительные правила:
- Используй TypeScript строгий режим, если это TypeScript проект
- Следуй принципам SOLID при написании кода
- Используй async/await вместо Promise.then()
- Обрабатывай все ошибки с помощью try-catch блоков
- Используй const вместо let, когда значение не изменяется

